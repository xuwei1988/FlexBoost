# Flexchar 隐私政策 — 信息填写模板

> 请在本文件中填写下方内容，完成后告知开发/Agent 生成 `privacy.html`。  
> 带 `[ ]` 的选项请保留一项并改为 `[x]`。  
> 「你的填写」列可直接编辑；若与「默认/建议」一致，可写 **同默认** 或留空。

---

## 1. 主体信息（必填）

| 项 | 默认/建议 | 你的填写 |
|----|-----------|----------|
| **公司/组织全称** | （例：FlexChar Technology Ltd.） | 同默认 |
| **联系邮箱**（隐私咨询） | （例：support@flexchar.com） | support@vertech-pro.com|
| **联系地址**（可选，无法提供可写 N/A） | （例：中国 XX 省 XX 市 …） | 中国广东省东莞市|
| **隐私政策生效日期** | （例：2026-05-23，YYYY-MM-DD） | 2026-05-23 |

---

## 2. 语言

请只保留一项为 `[x]`：

- [ ] **仅英文**（与当前 `index.html` 一致；App Store 常用）
- [ ] **仅中文**
- [x] **中英双语**（同一页上下排列）
- [ ] **中英分页**（如 `/privacy` + `/privacy-zh`）

补充说明（可选）：

```
（例如：对外用英文，内部团队需要中文版）
```

---

## 3. App 名称与产品描述

| 项 | 默认/建议 | 你的填写 |
|----|-----------|----------|
| **App 显示名** | Flexchar（与 iOS `Info.plist` 一致） | |
| **一句话产品定位** | 通过蓝牙（BLE）连接 FlexChar / ESP32-S3 充电设备，用于设备连接、状态查看、历史记录、参数配置及固件升级。 | |
| **目标用户**（可选） | 设备使用者、现场技术人员、开发与联调人员 | |

需要修改的表述（可选）：

```
（例如：不要提 ESP32-S3、改成 FlexChar Charger 等）
```

---

## 4. App 数据行为确认

请对每条选 **是 / 否 / 需修改**，并在「备注」中说明修改内容。  
若与默认一致，可整段写 **全部同默认**。

| # | 陈述 | 默认 | 你的确认 | 备注 |
|---|------|------|----------|------|
| 4.1 | App **无用户注册/登录** | 是 | | |
| 4.2 | 充电历史、CAN 数据、设备信息 **只存储在用户手机本地** | 是 | | |
| 4.3 | App 会连接 **`https://flex-boost.vercel.app`** 检查/下载固件 | 是 | | |
| 4.4 | **不会**将用户充电/历史数据上传到你们的业务服务器 | 是 | | |
| 4.5 | **没有** Firebase、Sentry、广告 SDK、跨 App 追踪（ATT） | 是 | | |
| 4.6 | 用户可主动通过系统 **分享/导出** 历史或日志 | 是 | | |
| 4.7 | 用户可从本机 **选择 .bin / .txt** 固件文件用于升级 | 是 | | |
| 4.8 | App 使用 **蓝牙** 扫描、连接设备（含后台蓝牙模式） | 是 | | |
| 4.9 | App 可能访问 **相册/文件**（仅用户主动选文件时） | 是 | | |
| 4.10 | App **不请求** 定位、相机、麦克风、通讯录权限 | 是 | | |

### 4.11 未来 6–12 个月内计划新增的能力（无则写「无」）

```
 无（例如：云账号同步、崩溃上报、用户反馈表单、推送通知、第三方支付等）
```

---

## 5. FlexBoost 网站 / API 行为确认

| # | 陈述 | 默认/建议 | 你的确认 | 备注 |
|---|------|-----------|----------|------|
| 5.1 | Vercel Blob 主要存储 **固件文件 + 版本元数据** | 是 | | |
| 5.2 | 固件 Dashboard（`index.html`）主要为 **内部管理**，不对公众开放 | 是 | | |
| 5.3 | App 调用 API 时 **不传输** 用户姓名、电话、Apple ID | 是 | | |
| 5.4 | 托管平台（Vercel）可能保留 **标准服务器/访问日志** | 建议写入政策 | | |
| 5.5 | 固件服务域名是否固定为 `flex-boost.vercel.app` | 是 | | |
| 5.6 | 是否有 **其他域名/镜像** 用于固件分发 | 无 | | |

其他网站/API 说明（可选）：

```
（例如：将来会换自定义域名 https://firmware.example.com）
```

---

## 6. App Store Connect「App 隐私」对齐（可选预填）

若你希望隐私政策网页与 App Store 问卷一致，请确认：

| 数据类型 | 建议申报 | 你的确认 |
|----------|----------|----------|
| 标识符 → 设备 ID（BLE） | 收集；用途：App 功能；不关联用户；不追踪 | |
| 使用数据 → 产品交互（设备 telemetry/历史） | 收集；用途：App 功能；不关联用户；不追踪 | |
| 联系信息 / 位置 / 财务 | 不收集 | |
| 是否用于追踪（Tracking） | 否 | |

补充（可选）：

```
（例如：法务要求额外声明某类数据）
```

---

## 7. 页面与部署偏好

请只保留需要的项为 `[x]`：

### 7.1 页面路径

- [x] `privacy.html` → `https://flex-boost.vercel.app/privacy.html` **（推荐）**
- [ ] `privacy/index.html` → `https://flex-boost.vercel.app/privacy`
- [ ] 其他：________________

### 7.2 页面样式

- [ ] 与现有 FlexBoost Dashboard 同风格（蓝白卡片）
- [x] 极简纯文本（审核够用、加载快）

### 7.3 站点链接

- [x] 在 `index.html` 底部增加 **Privacy Policy** 链接
- [ ] 不需要在 dashboard 加链接

### 7.4 提交 App Store 用的最终 URL

填写你计划填到 App Store Connect 的地址（可与 7.1 相同）：

```
https://flex-boost.vercel.app/privacy.html
```

---

## 8. 合规与受众（可选）

| 项 | 默认/建议 | 你的填写 |
|----|-----------|----------|
| App 是否面向 **13 周岁以下儿童** | 否 | |
| 是否需要 **GDPR（欧盟）** 专段 | 否（可先不写） | |
| 是否需要 **CCPA（加州）** 专段 | 否（可先不写） | |
| 政策更新时如何通知用户 | 更新网页生效日期；重大变更可在 App 内提示 | |

---

## 9. 快速提交（懒人模式）

若以上大部分与默认一致，可直接填写下面 4 行并提交：

```
公司名：FlexChar Technology Ltd.
联系邮箱：support@vertech-pro.com
语言：（英文 / 中文 / 双语）  双语
其余：全部同默认；页面 privacy.html + index 底部链接
```

---

## 10. 填写完成后

- [x] 我已完成本文件填写  
- [x] 请根据本文件生成 `privacy.html`  
- [x] 生成后请告诉我 App Store Connect 应填写的 **隐私政策 URL**

填写人：support

填写日期：2026.05.23
